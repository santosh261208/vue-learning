# Learning Journal

## 20.03.2026

### What I did
- Set up complete Vue.js development environment
- Installed Node.js and WebStorm
- Created new GitHub repo: vue-learning
- Created first Vue project using WebStorm (Vue.js template)
- Connected project to GitHub
- Cleared App.vue and wrote first "Hallo Welt" component

### What I learned
- Vue projects are created via WebStorm or `npm init vue@latest` in CMD
- `npm install` downloads all dependencies into node_modules
- `npm run dev` starts the development server with Hot-Reload
- `npm run build` creates a production build in ./dist
- Do NOT add README when creating GitHub repo if you already have local files

### Setup Commands
- `npm install` → install dependencies
- `npm run dev` → start dev server (localhost:5173)
- `npm run build` → production build

### Git in WebStorm
- `Ctrl + K` → Commit
- `Ctrl + Shift + K` → Push

### Next steps
- Learn Vue template syntax
- Build first real component